// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Video {
  id          String   @id @default(cuid())
  title       String
  fileName    String   @unique
  filePath    String   @unique
  duration    Float?   // in seconds
  fileSize    BigInt?  // in bytes
  thumbnail   String?  // base64 encoded thumbnail
  episode     Int?     // episode number if applicable
  season      String?  // season identifier
  genre       String?  // comma-separated genres
  year        Int?     // release year
  rating      Float?   // user rating 0-10
  lastWatched DateTime? // last time watched
  watchTime   Float?   // current watch position in seconds
  watchProgress Float @default(0) // watch progress percentage (0-100)
  isLiked     Boolean @default(false) // like status
  likedAt     DateTime? // when it was liked
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("videos")
}
